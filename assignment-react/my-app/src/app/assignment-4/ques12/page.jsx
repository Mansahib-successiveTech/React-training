import DataTable from "@/components/DataTableMui";

const Home = () => {
  return (
    <>
      <p>
        12.Build a data table using Material-UI's Table component. Populate the
        table with sample data and add features like sorting and pagination.
      </p>
      <DataTable />
    </>
  );
};
export default Home;
